import React from 'react';
import { render } from '@testing-library/react';
import { NumberList } from '../01 NumberList';

test('if a number can be added to the list', () => {
  render(<NumberList />);
  
})
